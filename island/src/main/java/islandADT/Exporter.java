package islandADT;

import ca.mcmaster.cas.se2aa4.a2.io.Structs;

public class Exporter {
    public static Structs.Mesh export_island(GeometryContainer geometryContainer) {
        //TODO change
        return Structs.Mesh.newBuilder().build();
    }
}
